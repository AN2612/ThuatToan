#include<iostream>
#include<unordered_set>
using namespace std;
int main(){
    int n,m;
    unordered_set<int> present, appear;
    cin >> n;
    for(int i = 0; i<n; i++){
        int x;
        cin >> x;
        present.insert(x);
    }
    cin >> m;
    for(int i = 0; i<m; i++){
        int x;
        cin >> x;
        if(present.count(x) || appear.count(x)){
            cout << "YES" << endl;
        }else{
            cout << "NO" <<endl;
            appear.insert(x);
        }
    }
}
